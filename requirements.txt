# Dependencies now live in pyproject.toml. Install the package (editable) with
# the extras you need instead of this file:
#
#   pip install -e .            # core: classical analysis + ingest + web server
#   pip install -e ".[ml]"      # + the multi-GB ML scoring/caption/face stack
#   pip install -e ".[dev]"     # + the test suite (pytest, httpx)
#   pip install -e ".[ml,dev]"  # everything
#
# A CUDA build of torch is strongly recommended for the [ml] path; see
# https://pytorch.org/get-started/locally/ for the right index URL.
#
# This file is kept only so `pip install -r requirements.txt` still bootstraps a
# working install (core + ml + dev). It mirrors pyproject.toml — edit deps there.
-e .[ml,dev]
